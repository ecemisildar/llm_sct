Best patrolling supervisors synchronized with target_approach_nomark.xml.

Selection: highest mean task progression, then full completion rate, successful completion time, and collisions. Selection compares both collision-control series for P1–P6; P7 is excluded.

For each prompt: K = Sync(original plant, evaluated supervisor, target_approach_nomark); S = SupC(original plant, K). Original evaluated controllers and their collision constraints are preserved. No simulation was run. An empty supervisor has no valid runtime YAML. See summary.json for sources and state counts.
